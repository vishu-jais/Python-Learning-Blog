#string reverse program 
#define a function named reverse_string that takes one argument 's'
def reverse_string(s):
 # if the length of the  string  is 1 or less ,just return  the string  itself 
  if len(s) <= 1:
        return s
    else:
        return reverse_string(s[1:]) + s[0]
# Ask the user to enter a string
text = input("Enter a string: ")
# Print the reversed string by calling the function reverse_string()
print("Reversed string:", reverse_string(text))
